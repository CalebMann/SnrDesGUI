package sample;


import javax.swing.*;

//This is the driver file. This being run will start the GUI

public class Driver
{
    public static void main(String[] args)
    {
        //Create the GUI and set its exit condition
        GUI gui = new GUI();
        gui.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
    }
}
